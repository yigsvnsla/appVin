import { NoArrowDirective } from './no-arrow.directive';

describe('NoArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new NoArrowDirective();
    expect(directive).toBeTruthy();
  });
});
